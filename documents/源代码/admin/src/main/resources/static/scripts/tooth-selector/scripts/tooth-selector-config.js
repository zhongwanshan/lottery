/** tooth selector */
var ToothSelectorConfig = {
    // 牙位图SVG的ID
    TeethSVGDomId: 'tooth_selector_svg',
    // 是否开启连冠选择
    ConnectorEnabled: true,
    // 选择的牙齿和修复项目的临时存储
    DataStore: udocadDataStore
};
